{
    "name": "semperton/database",
    "description": "A compact PDO / SQLite wrapper library.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=7.2.0"
    },
    "require-dev": {
        "ext-pdo": "*",
        "ext-sqlite3": "*",
        "ext-pdo_sqlite": "*",
        "vimeo/psalm": "^4.8",
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "psr-4": {
            "Semperton\\Database\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit tests",
        "check": "psalm --no-cache"
    }
}
